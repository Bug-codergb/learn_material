let obj = {
  name: "gblina",
  age:12
}
// Object.seal(obj); //seal方法使对象不可删除，创建新属性，但是可以修改已有属性值
// Object.preventExtensions(obj);//preventExtensions方法使对象不可添加新属性，但是可以修改，删除已有属性
// Object.freeze(obj);//freezr方法使对象完全不可配置，不可删除，不可修改，不可添加新属性

